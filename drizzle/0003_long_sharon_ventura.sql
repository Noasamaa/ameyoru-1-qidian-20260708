-- This migration is a no-op marker.
-- Schema snapshot has been brought up to date with the live MySQL state,
-- which was previously evolved through manual ALTERs outside drizzle (gift payment fields).
-- Future generate calls should produce empty diffs.
SELECT 1;
